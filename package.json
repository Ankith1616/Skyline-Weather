{
  "name": "weather-app-root",
  "version": "1.0.0",
  "description": "Root workspace for the production-ready Full Stack Weather App",
  "private": true,
  "scripts": {
    "install:all": "npm.cmd install --prefix backend && npm.cmd install --prefix frontend",
    "dev:backend": "npm.cmd run dev --prefix backend",
    "dev:frontend": "npm.cmd run dev --prefix frontend",
    "dev": "npx concurrently \"npm.cmd run dev:backend\" \"npm.cmd run dev:frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
